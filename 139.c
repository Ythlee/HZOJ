#include <stdio.h>

int main(int argc, char *argv[])
{
    int n = scanf("%d", &n);
    for(int i = 1; i <= n; i++) {
        if(i < (n + 1) / 2 + 1) {
            int m = n;
            for(int j = 1; j < m - i; j++) {

            }
        }
    }
    return 0;
}
